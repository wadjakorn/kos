{
  "_comment": "Copy to config/paths.json to point the engine at an external data root ('the db'). Resolution order: KOS_DATA_ROOT env var > config/paths.json > repo root (default).",
  "_note": "data_root is the single entry point: KOS data dir, its own git repo, and the Obsidian vault root. Leading ~ is expanded.",

  "data_root": "~/knowledge-base-data"
}
